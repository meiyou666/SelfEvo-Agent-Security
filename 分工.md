更均衡的四人分法建议改成这样：

| 人员 | 主责                                           | 副责                   | 工作量特点       |
| ---- | ---------------------------------------------- | ---------------------- | ---------------- |
| 1 号 | Runner / config / run 目录 / 统一日志入口      | report 生成骨架        | 持续中等偏重     |
| 2 号 | Agent runtime / tools wrapper / dry-run 工具   | policy 接口            | Sprint 1、3 较重 |
| 3 号 | shadow memory / provenance / retrieval context | provenance policy 规则 | Sprint 2 较重    |
| 4 号 | tasks / fixtures / 样本安全 / metrics          | 实验报告内容整理       | Sprint 1、4 较重 |

具体推进可以这样调：

**Sprint 0：四人共同完成**

- 1 号：定 `run_metadata`、目录结构。
- 2 号：定 tool event schema。
- 3 号：定 memory event / provenance schema。
- 4 号：定 task/sample metadata 和 ASR 统计口径。

**Sprint 1：最小闭环**

- 1 号：`config.py`、`run_infection.py`、`run_trigger.py`、run id、日志目录。
- 2 号：最小 Agent、`read_url` fixture、`execute_command` dry-run。
- 3 号：`write_memory()`、`retrieve_memory()`、`memory_events.jsonl`。
- 4 号：3 个 infection、3 个 trigger、最小 `metrics.py` 和 `report.md`。

**Sprint 2：provenance 闭环**

- 3 号主责：`memory_id`、`retrieved_memory_ids`、`contains_untrusted_memory`。
- 2 号配合：把 retrieval context 接进 tool call。
- 1 号配合：保证所有 event 有同一个 `run_id` 和 `task_id`。
- 4 号配合：补齐样本 metadata，并让 metrics 能读 provenance 字段。

**Sprint 3：策略对照**

- 2 号主责：policy mode、dry-run/block 规则。
- 4 号主责：`attempted_asr`、`pre_policy_asr`、`post_policy_asr`、`false_positive_rate`。
- 1 号：统一跑 G1/G2/G3。
- 3 号：保证 policy 能拿到 memory provenance。

**Sprint 4：扩展实验**

- 4 号：扩样本、扩任务、整理典型案例。
- 1 号：批量运行和聚合目录。
- 2 号：多模型配置或 Agent 参数统一。
- 3 号：保证 memory/provenance schema 稳定。



下面是按今天到 **7 月 5 日** 倒排的 Sprint 安排。

| Sprint                  | 参考时间  | 核心目标                   | 成功标准                                                     |
| ----------------------- | --------- | -------------------------- | ------------------------------------------------------------ |
| Sprint 0：口径对齐      | 6.19      | 冻结安全边界、schema、分工 | 明确 offline、dry-run、三类 ASR、shadow memory；确定目录结构和 JSONL 最小字段 |
| Sprint 1：最小闭环      | 6.20-6.24 | 让系统第一次跑起来         | 能运行 infection 和 trigger；生成 run 目录；写入 memory/tool/task 日志；输出最小 report |
| Sprint 2：补 provenance | 6.25-6.28 | 让工具调用能追溯到记忆来源 | 每条 memory 有 `memory_id`；trigger 检索能记录 `retrieved_memory_ids`；tool call 能关联 retrieval context |
| Sprint 3：策略对照      | 6.29-7.1  | 验证 policy 是否降低风险   | 跑出 G1/G2/G3 三组；报告包含 `attempted_asr`、`pre_policy_asr`、`post_policy_asr`、`block_rate` |
| Sprint 4：扩展实验      | 7.2-7.3   | 从 demo 变成可展示实验结果 | 增加任务和样本数量；固定 dataset version；产出聚合指标表和典型案例 |
| Sprint 5：最终交付      | 7.4-7.5   | 打包、文档、演示、兜底     | 一条命令可复现核心流程；最终报告完整；README 写清运行方式；准备展示材料 |

更具体一点：

**Sprint 0：6.19**
今天只做对齐，不要急着写复杂代码。

必须完成：

- 确认项目只做防御实验，不做真实攻击。
- 确认默认 `offline`。
- 确认 `execute_command` 永远先 dry-run。
- 确认 ASR 拆成三类。
- 确认 shadow memory 是指标真相源。
- 确认四个人分工和接口边界。

成功标准：
到今天结束，四个人都知道自己第一版要交哪些文件、日志字段长什么样、什么行为绝对不能做。

**Sprint 1：6.20-6.24**
目标不是漂亮，而是跑通。

必须完成：

- `config.py`
- `run_infection.py`
- `run_trigger.py`
- `agent/tools.py` 里的 dry-run 工具
- `agent/memory_backend.py` 的 `write_memory()` / `retrieve_memory()`
- `lab/tasks.py` / `lab/poison.py` 的最小样本
- `lab/metrics.py` 的最小统计

成功标准：

```
python run_infection.py
python run_trigger.py
python lab/metrics.py logs/runs/<run_id>
```

能跑完，并生成：

- `run_metadata.json`
- `memory_events.jsonl`
- `tool_calls.jsonl`
- `policy_events.jsonl`
- `task_events.jsonl`
- `report.md`

**Sprint 2：6.25-6.28**
这一阶段补项目的核心价值：provenance。

必须完成：

- 每条 memory 有 `memory_id`。
- memory 记录 `source_id`、`trust_level`、`risk_tags`。
- 每次 retrieval 记录 `retrieved_memory_ids`。
- tool call 日志能记录 `derived_from_memory_ids` 或 `retrieval_context_id`。
- 如果无法精确证明来源，标记 `potentially_derived`，不要假装精确因果。

成功标准：
看到一次风险工具调用时，能回答：

```
它发生在哪个 run？
哪个 task？
trigger 前检索到了哪些 memory？
这些 memory 是否来自 untrusted source？
policy 为什么 allow / dry_run / block？
```

**Sprint 3：6.29-7.1**

必须完成三组实验：

| 组别 | 内容                                |
| ---- | ----------------------------------- |
| G1   | benign memory + no policy           |
| G2   | polluted memory + no policy/audit   |
| G3   | polluted memory + provenance policy |

成功标准：
报告中至少能展示：

- G2 比 G1 更容易出现风险意图；
- G3 相比 G2，`post_policy_asr` 下降；
- policy 的 block/dry-run 决策有日志可追踪。

**Sprint 4：7.2-7.3**
这两天可以投入更多时间，用来把结果做厚。

必须完成：

- infection tasks 扩到至少 5 个。
- trigger tasks 扩到至少 5 个。
- benign control 至少 3 个。
- synthetic untrusted contents 至少 3 个。
- 输出一张汇总表：不同组别下的 ASR、block rate、false positive rate。

成功标准：
项目不再只是“跑通 demo”，而是能拿出一组像样的实验结果。

**Sprint 5：7.4-7.5**
最后两天不要再开发新功能，主要做交付。

必须完成：

- README：写清环境、运行命令、目录结构。
- final report：写清研究问题、方法、实验设置、指标、结果、局限。
- demo 流程：准备一条最稳的展示路径。
- 清理样本：确认没有真实攻击 payload、真实外泄地址、真实命令执行。
- 兜底：保留一份可稳定运行的最小数据集。

成功标准：
7 月 5 日提交时，别人可以按 README 跑出核心结果；即使扩展实验有瑕疵，最小闭环也必须稳定可复现。