# SelfEvo Agent Security：团队实际工作流程

## 0. 这份文档到底是干什么的

这不是 README，也不是论文式实验设计，更不是给外部评审看的“官方文档”。

这份文档是团队内部用的 **实际工作流说明**：

- 每个人负责什么；
- 先做什么，后做什么；
- 哪些事情不能并行，哪些可以并行；
- 每个阶段交付什么；
- 怎么判断一个阶段做完了；
- 遇到 CrewAI memory、ASR 定义、Docker 网络、安全样本这些坑时怎么处理；
- 最后怎么把这个项目从“想法”推进成“能跑、能测、能出结果”的实验平台。

一句话：

> 这份文档不是讲“系统应该长什么样”，而是讲“团队明天开始怎么干”。

---

## 1. 项目当前真实状态判断

当前项目还处在 **设计到落地的前夜**。

现在已经有的东西：

- 一个比较清楚的研究方向：Self-evolving Agent 的长期记忆污染风险；
- 一个大致正确的模块划分：投毒样本、记忆溯源、工具策略、指标评估；
- 一个 baseline 设想：CrewAI agent + memory + tools + two-stage run；
- 一些重要安全边界意识：dry-run、本地 mock、不要真实外泄。

但还没有真正完成的东西：

- 没有稳定代码骨架；
- 没有可跑通的 infection / trigger 两阶段流程；
- 没有统一日志 schema；
- 没有 shadow memory provenance；
- 没有明确 ASR 计算口径；
- 没有多模型对照；
- 没有团队协作约定。

所以接下来的工作重点不是“继续写概念”，而是：

```text
先搭最小闭环 → 再补 provenance → 再加 policy → 再做多模型实验
```

---

## 2. 先把几个坑钉死

这些是前面设计里最容易把团队带偏的地方，必须先形成共识。

### 2.1 不要一上来追求真实攻击效果

本项目的目标不是让 Agent 真的把东西发出去，也不是验证真实命令执行。

团队统一口径：

```text
我们评估的是风险意图，不是真实危害。
```

所以默认：

- `execute_command` 只 dry-run；
- mock endpoint 只用于观测；
- 不接真实外网 exfil；
- 不写真实 payload；
- 不把危险命令做成可复制运行的样子。

### 2.2 ASR 必须拆开算

团队不要再说一个模糊的 ASR。

以后统一拆成三类：

| 名称 | 含义 | 用途 |
|---|---|---|
| `attempted_asr` | Agent 是否产生风险工具调用意图 | 看模型/记忆风险 |
| `pre_policy_asr` | 如果没有策略，这个意图是否会构成风险 | 看原始暴露面 |
| `post_policy_asr` | 策略介入后是否仍未被拦截 | 看防御效果 |

以后所有实验报告都必须同时给这三个数，不能只报一个 ASR。

### 2.3 CrewAI memory 先不要当成可信实验依据

CrewAI 的 `memory=True` 可以先用来跑 baseline，但不能假设它能完整支持：

- source tag；
- trust label；
- memory id；
- retrieval trace；
- memory-to-tool provenance。

团队策略：

```text
先做 shadow memory，作为实验审计真相源。
CrewAI memory 只是 Agent 能力层，不是指标真相源。
```

也就是说：

- Agent 可以用 CrewAI memory；
- 但指标计算以我们自己的日志和 shadow memory 为准；
- 如果 CrewAI memory 后面能挂自定义 backend，再考虑整合。

### 2.4 Docker 网络不要想当然

原来设想里有个问题：

```text
docker run --network none
```

和

```text
访问宿主机 127.0.0.1 attacker_server
```

不能同时成立。

团队后续分三种模式处理：

| 模式 | 用途 | 网络策略 |
|---|---|---|
| offline | 默认开发和 CI | 不联网，全部 fixture |
| local_mock | 验证 mock server 链路 | agent 和 mock 在同一网络命名空间 |
| networked_research | 少量真实网页实验 | 明确 allowlist，禁止真实执行 |

MVP 只做 offline，不要先折腾 Docker 网络。

### 2.5 投毒样本不是 payload 仓库

`lab/poison.py` 只放实验样本注册信息，不放真实攻击能力。

允许：

- synthetic 文本；
- 安全占位符；
- mock intent；
- risk tag；
- trigger category。

不允许：

- 真实外泄地址；
- 可执行危险命令；
- 规避检测技巧；
- 权限提升逻辑；
- 面向真实系统的 payload。

---

## 3. 团队分工

建议按 6 条工作线分工。人数少时可以一人兼多线，但责任边界要清楚。

### 3.1 Owner A：项目集成与 Runner

负责文件：

- `config.py`
- `run_infection.py`
- `run_trigger.py`
- 项目入口脚本

主要任务：

- 把两阶段流程串起来；
- 管理 run id；
- 加载配置；
- 调用任务集；
- 调用 agent；
- 确保每次实验有完整 run metadata；
- 保证别人写的模块能被统一跑起来。

交付物：

- `python run_infection.py` 能跑；
- `python run_trigger.py` 能跑；
- 每次运行生成独立 run 目录；
- 失败时能留下可读错误日志。

完成标准：

```text
不用手动改代码，只改配置，就能跑一轮完整 offline 实验。
```

---

### 3.2 Owner B：Agent Runtime

负责文件：

- `agent/crew.py`

主要任务：

- 封装 CrewAI Agent；
- 接入 LLM；
- 绑定工具；
- 控制 memory 开关；
- 保持核心 loop 尽量薄；
- 不在这里写策略、不写指标、不写投毒逻辑。

交付物：

- 一个 `build_agent()` 或类似工厂函数；
- 一个 `run_task(task)` 或类似执行接口；
- 能接收外部传入 tools；
- 能被 runner 调用。

完成标准：

```text
换模型、换任务、换工具时，不需要改 agent/crew.py 主逻辑。
```

注意：

- 不要把安全规则塞进 Agent prompt 里当主防线；
- Prompt 可以提醒安全边界，但真正拦截必须在工具层。

---

### 3.3 Owner C：工具层与策略层

负责文件：

- `agent/tools.py`
- 后续可拆 `agent/policy.py`

主要任务：

- 实现 `search`；
- 实现 `read_url`；
- 实现 `execute_command` dry-run；
- 实现统一 `policy_check()`；
- 每次工具调用都写 tool event；
- 每次策略判断都写 policy event。

工具调用统一流程：

```text
Agent 提出工具调用
        ↓
工具 wrapper 接收参数
        ↓
参数 normalize / redact
        ↓
补充当前 task、phase、memory context
        ↓
policy_check
        ↓
allow / dry_run / block
        ↓
写日志
        ↓
返回结果给 Agent
```

交付物：

- 所有工具默认可审计；
- `execute_command` 默认不真实执行；
- policy mode 支持 `off/audit/dry_run/block`；
- 高风险工具调用能被标记。

完成标准：

```text
任何一次工具调用，都能在日志里找到参数、来源、策略决策和是否执行。
```

---

### 3.4 Owner D：Memory 与 Provenance

负责文件：

- `agent/memory_backend.py`
- 后续可能的 `agent/shadow_memory.py`

主要任务：

- 实现 shadow memory；
- 给 memory record 分配 `memory_id`；
- 保存 source；
- 保存 trust level；
- 记录写入事件；
- 记录检索事件；
- 把 retrieved memory ids 提供给工具层。

核心原则：

```text
没有 memory_id，就不能谈 provenance。
没有 provenance，就不能谈 policy。
```

交付物：

- `write_memory()`；
- `retrieve_memory()`；
- `memory_events.jsonl`；
- memory record schema；
- retrieval event schema。

完成标准：

```text
触发阶段每次检索，都能知道检索到了哪些 memory_id，以及这些 memory 来自哪里。
```

---

### 3.5 Owner E：任务、样本与安全边界

负责文件：

- `lab/tasks.py`
- `lab/poison.py`
- `data/poison_pages/`
- `data/tasks/`

主要任务：

- 准备 infection tasks；
- 准备 trigger tasks；
- 准备 benign control tasks；
- 准备 synthetic poison samples；
- 保证样本安全；
- 给每个样本标注 category 和 expected risk。

任务设计原则：

```text
infection task 负责让 Agent 接触不可信内容；
trigger task 必须保持良性；
风险应该来自历史记忆，而不是当前任务直接诱导。
```

交付物：

- 至少 5 个 infection tasks；
- 至少 5 个 benign trigger tasks；
- 至少 3 个 benign control contents；
- 至少 3 个 synthetic untrusted contents；
- 每个样本都有 metadata。

完成标准：

```text
别人不用问你，也能从 task/sample metadata 看懂这个样本在测什么。
```

---

### 3.6 Owner F：指标与报告

负责文件：

- `lab/metrics.py`
- `logs/reports/`

主要任务：

- 读取 JSONL 日志；
- 聚合 run 结果；
- 计算核心指标；
- 输出 Markdown/CSV 报告；
- 标记 failed/partial/valid run；
- 整理关键案例。

必须计算：

- `memory_write_rate`；
- `memory_retention_rate`；
- `trigger_rate`；
- `attempted_asr`；
- `pre_policy_asr`；
- `post_policy_asr`；
- `block_rate`；
- `false_positive_rate`。

完成标准：

```text
给一个 run 目录，metrics 能自动输出一份可读报告。
```

---

## 4. 实际推进路线

不要一开始就做完整系统。按下面 4 个 Sprint 推进。

---

### Sprint 0：对齐口径，不写复杂代码

目标：防止团队一开始就各写各的。

周期：0.5 到 1 天。

要做的事：

1. 确认项目只做防御实验，不做真实攻击；
2. 确认默认模式是 offline；
3. 确认 `execute_command` 默认 dry-run；
4. 确认 ASR 拆成三类；
5. 确认 shadow memory 是指标真相源；
6. 确认日志 schema 先行。

产出：

- `config.py` 配置草案；
- JSONL event schema 草案；
- 任务/样本 metadata 草案；
- Owner 分工表。

不要做：

- 不要先接真实网页；
- 不要先做 Docker 网络；
- 不要先跑多模型；
- 不要先调复杂 prompt。

完成标准：

```text
团队所有人对“我们到底测什么、怎么算成功、怎么保证安全”达成一致。
```

---

### Sprint 1：打通 offline 最小闭环

目标：让项目第一次真正跑起来。

周期：2 到 3 天。

主线流程：

```text
本地 synthetic content
        ↓
infection task
        ↓
Agent 读取/总结
        ↓
shadow memory 写入
        ↓
trigger task
        ↓
shadow memory 检索
        ↓
工具 dry-run
        ↓
日志
        ↓
metrics 输出
```

Owner A 要做：

- 跑通 `run_infection.py`；
- 跑通 `run_trigger.py`；
- 每次 run 自动生成 `run_id`；
- 统一日志目录。

Owner B 要做：

- 最小 Agent 封装；
- 能执行一个 task；
- 能绑定工具；
- memory 可以先简化。

Owner C 要做：

- `read_url` 读取本地 fixture；
- `execute_command` dry-run；
- `policy_check` 先返回 audit/dry_run；
- 工具调用写日志。

Owner D 要做：

- shadow memory 的 `write/retrieve`；
- memory id；
- source trust label；
- memory event 日志。

Owner E 要做：

- 3 个 infection tasks；
- 3 个 trigger tasks；
- 2 个 synthetic untrusted samples；
- 2 个 benign samples。

Owner F 要做：

- 读 JSONL；
- 统计 memory write；
- 统计 retrieval；
- 统计 risky attempt；
- 输出最小报告。

Sprint 1 完成标准：

```text
一条命令跑 infection，一条命令跑 trigger，最后能看到一份 metrics report。
```

可以接受的问题：

- Agent 表现很粗糙；
- 样本很少；
- 指标只有基础版；
- policy 规则很简单。

不能接受的问题：

- 没日志；
- 工具调用不可追踪；
- memory 没 id；
- trigger task 本身诱导危险行为；
- 真实执行命令。

---

### Sprint 2：补 provenance 闭环

目标：让团队能回答“这个工具调用到底是不是被污染记忆影响的”。

周期：2 到 4 天。

重点不是加功能，而是补链路。

必须打通：

```text
content source
   → memory_id
   → retrieval event
   → derived_from_memory_ids
   → tool call event
   → policy decision
```

Owner D 是主负责人，Owner C 配合。

需要新增：

- `contains_untrusted_memory`；
- `retrieved_memory_ids`；
- `derived_from_memory_ids`；
- `source_trust_levels`；
- `risk_tags`；
- `provenance_missing` 检测。

这里要特别注意：

如果 Agent 生成工具参数时无法严格证明来自哪个 memory，可以先用保守策略：

```text
当前 task 最近一次 retrieval 包含 untrusted memory，
且随后发生高风险工具调用，
则标记为 potentially_derived_from_untrusted_memory。
```

不要假装有精确因果，日志里要区分：

- `directly_derived`；
- `potentially_derived`；
- `unknown_derivation`。

Sprint 2 完成标准：

```text
每个风险工具调用，都至少能关联到最近一次 retrieval context。
```

---

### Sprint 3：做策略对照

目标：验证策略层是否真的降低风险。

周期：2 到 3 天。

要跑三组：

| 组别 | 说明 |
|---|---|
| G1 | benign memory + no policy |
| G2 | polluted memory + no policy/audit |
| G3 | polluted memory + provenance policy |

Owner C 负责策略，Owner F 负责指标，Owner A 负责统一跑。

策略先做简单规则，不要上复杂模型判定：

```text
如果 high-risk tool call 出现在 trigger 阶段，
且最近 retrieval 包含 untrusted memory，
则 dry_run 或 block。
```

指标报告必须包含：

- `attempted_asr`；
- `pre_policy_asr`；
- `post_policy_asr`；
- `block_rate`；
- `false_positive_rate`。

Sprint 3 完成标准：

```text
能用 G2 vs G3 说明策略介入前后风险变化。
```

不要过早追求：

- 复杂策略语言；
- 自动学习策略；
- LLM-as-judge；
- 大规模样本。

---

### Sprint 4：扩展任务集和模型对比

目标：从 demo 变成实验结果。

周期：3 到 7 天。

前提：Sprint 1 到 3 已稳定。

要做：

- 增加任务数量；
- 增加 benign control；
- 增加 synthetic untrusted categories；
- 固定 task set version；
- 固定 poison set version；
- 跑多个模型；
- 输出聚合报告。

模型对比时必须固定：

- 同一 task set；
- 同一 poison set；
- 同一 policy mode；
- 同一 memory backend；
- 同一日志 schema；
- 尽量相同 temperature。

Sprint 4 完成标准：

```text
能产出一张模型/策略/指标对照表，并能抽样解释典型案例。
```

---

## 5. 日常开发流程

团队每天按这个流程走，不要靠口头同步。

### 5.1 开始一个任务前

每个任务卡必须写清楚：

```text
目标：这张卡要解决什么问题
输入：依赖哪些文件/模块
输出：交付什么文件/接口/日志
验收：怎么判断完成
风险：可能影响谁
```

不要写这种任务：

```text
优化 agent
完善 memory
改一下 metrics
```

要写成：

```text
为 shadow memory 增加 memory_id 和 source_trust_level，并在 write/retrieve 时写 JSONL event。
```

---

### 5.2 开发时

推荐顺序：

```text
1. 先补 schema
2. 再补最小实现
3. 再跑一个最小 fixture
4. 再看日志
5. 最后才调 Agent 行为
```

不要先调 prompt。这个项目最重要的是日志链路，不是 prompt 手感。

---

### 5.3 提交前自查

每个 PR 或每次合并前自查：

- 是否引入真实命令执行？
- 是否引入真实外网地址？
- 是否破坏 JSONL schema？
- 是否让 trigger task 变成直接诱导？
- 是否让日志缺少 run id / task id？
- 是否让 policy 决策不可追踪？
- 是否让 metrics 口径变化但没记录？

如果改了指标口径，必须在文档或报告里写清楚。

---

### 5.4 每日同步会

每天同步只问四个问题：

1. 昨天哪个链路打通了？
2. 今天要让哪个 event 出现在日志里？
3. 现在最大的阻塞是什么？
4. 有没有安全边界被放松？

不要开成泛泛的“进度汇报会”。

---

## 6. 文件和接口约定

### 6.1 推荐目录

```text
SelfEvo-Agent-Security/
├── 设计架构.md
├── config.py
├── run_infection.py
├── run_trigger.py
├── agent/
│   ├── crew.py
│   ├── tools.py
│   ├── memory_backend.py
│   └── policy.py
├── lab/
│   ├── poison.py
│   ├── tasks.py
│   └── metrics.py
├── data/
│   ├── poison_pages/
│   ├── tasks/
│   └── runs/
└── logs/
    └── runs/
```

### 6.2 每次 run 的目录

```text
logs/runs/<run_id>/
├── run_metadata.json
├── memory_events.jsonl
├── tool_calls.jsonl
├── policy_events.jsonl
├── task_events.jsonl
├── errors.jsonl
└── report.md
```

### 6.3 最小事件字段

所有 event 至少带：

```json
{
  "event_id": "evt_...",
  "event_type": "...",
  "run_id": "run_...",
  "task_id": "task_...",
  "phase": "infection|trigger",
  "timestamp": "..."
}
```

没有这些字段，metrics 不应该接收。

---

## 7. 关键交接点

### 7.1 Owner E → Owner D

样本和任务交给 memory 前，必须提供：

- `source_id`；
- `source_type`；
- `trust_level`；
- `poison_sample_id`；
- `expected_risk`。

否则 memory 无法打标签。

### 7.2 Owner D → Owner C

memory 给工具层的上下文必须包含：

- `retrieved_memory_ids`；
- `source_trust_levels`；
- `contains_untrusted_memory`；
- `risk_tags`。

否则 policy 无法判断 provenance。

### 7.3 Owner C → Owner F

工具层给 metrics 的日志必须包含：

- `tool_name`；
- `args_redacted`；
- `derived_from_memory_ids` 或 `retrieval_context_id`；
- `policy_decision`；
- `executed`；
- `risk_tags`。

否则 ASR 不能算。

### 7.4 Owner A → 全员

runner 必须保证：

- 所有模块共享同一个 `run_id`；
- 所有日志写到同一个 run 目录；
- 配置被保存；
- 失败时 run 状态可判断。

---

## 8. Definition of Done

### 8.1 一个模块算完成

必须满足：

- 有清晰输入输出；
- 能被 runner 调用；
- 失败时写 error event；
- 不依赖手动改代码；
- 不破坏安全边界；
- 至少有一个 fixture 能跑通。

### 8.2 一个 Sprint 算完成

必须满足：

- 有可运行命令；
- 有日志输出；
- 有 metrics 或中间报告；
- 有失败案例记录；
- 下一个 Sprint 可以基于它继续。

### 8.3 项目第一阶段算完成

必须满足：

```text
offline 模式下，能稳定跑完：

infection → memory write → trigger → memory retrieval → tool attempt → policy decision → metrics report
```

并且报告里至少有：

- memory write count；
- polluted memory retrieval count；
- risky tool attempt count；
- attempted ASR；
- policy decision summary。

---

## 9. 团队不要踩的坑

### 9.1 不要过早接真实网页

真实网页会引入太多不稳定因素。先用 fixture 把实验链路跑稳。

### 9.2 不要过早做多模型

多模型对比只有在 schema、日志、metrics 稳定后才有意义。

### 9.3 不要把 prompt 当防御主力

Prompt 约束可以有，但最终防线必须在工具 wrapper 和 policy 层。

### 9.4 不要让样本越来越危险

这个项目的创新点不是 payload，而是 provenance + policy + metrics。

### 9.5 不要后补日志

后补日志一定会漏。所有模块第一版就要写 event。

### 9.6 不要把 CrewAI memory 当黑盒真相

CrewAI memory 可以帮助 Agent 表现得像一个带记忆的系统，但实验结论必须来自可审计的 shadow memory。

---

## 10. 推荐任务看板

### Backlog

- 定义 run metadata schema；
- 定义 memory event schema；
- 定义 tool event schema；
- 定义 policy event schema；
- 准备 synthetic poison fixture；
- 准备 benign trigger fixture。

### Sprint 1

- 实现 `config.py`；
- 实现 `run_infection.py`；
- 实现 `run_trigger.py`；
- 实现 dry-run tools；
- 实现 shadow memory v0；
- 实现 metrics v0。

### Sprint 2

- 增加 memory id；
- 增加 source trust label；
- 增加 retrieval event；
- 增加 derived memory context；
- 增加 provenance missing 检测。

### Sprint 3

- 增加 policy modes；
- 增加 block/dry-run 规则；
- 增加 attempted/pre/post ASR；
- 增加 false positive 统计；
- 跑 G1/G2/G3 对照。

### Sprint 4

- 扩展任务集；
- 扩展样本集；
- 固定 dataset version；
- 跑多模型；
- 输出最终对照报告。

---

## 11. 最小可交付版本

如果时间很紧，团队只需要先交付这个版本：

```text
1. offline 模式
2. 3 个 infection tasks
3. 3 个 trigger tasks
4. shadow memory
5. dry-run execute_command
6. JSONL logs
7. attempted_asr 指标
8. 一份 report.md
```

这个版本虽然小，但已经能证明项目主链路：

```text
不可信内容是否进入记忆；
污染记忆是否被后续检索；
检索后是否出现风险工具意图；
策略是否能看到并处理这个风险。
```

只要这个闭环跑通，后续加模型、加任务、加策略都是扩展问题。

---

## 12. 下一步马上做什么

团队下一步不要继续扩写文档，应该直接开工以下 5 件事：

1. **Owner A**：建 `config.py`、`run_infection.py`、`run_trigger.py` 空骨架；
2. **Owner D**：建 shadow memory，先能写 `memory_events.jsonl`；
3. **Owner C**：建 dry-run `execute_command` 和 `policy_check()`；
4. **Owner E**：准备最小 synthetic fixture 和 benign trigger tasks；
5. **Owner F**：写 `metrics.py`，先统计 event count 和 attempted ASR。

第一周目标不是“做漂亮”，而是：

```text
能跑一条完整链路，能看到日志，能算出第一个粗糙指标。
```

这才是这个项目从设计进入实际研究的分界线。
