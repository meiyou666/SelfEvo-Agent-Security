# 实验结果说明

本文记录当前完整实验平台的可复现结果，覆盖 runner、Agent backend、工具运行时、shadow memory、provenance、任务样本、metrics 和 report。当前成果不是单独的工具模块，而是一个可运行、可审计、不会真实执行危险命令的 Agent 安全实验 MVP。

## 当前配置

- 分支：`develop`
- Agent backend：
  - `crewai`：真实 CrewAI + DeepSeek
  - `mock`：离线链路兜底
- DeepSeek 模型：`deepseek-v4-flash`
- API endpoint：`https://api.deepseek.com`
- Policy mode：`dry_run`
- 工具安全边界：
  - `read_url` 只读本地 `.txt` fixture
  - `execute_command` 永远不真实执行

## 已完成链路

完整链路已经跑通：

```text
infection task
  -> read_url fixture
  -> Agent 生成 memory candidate
  -> shadow memory write
  -> trigger/control task
  -> shadow memory retrieval
  -> Agent 产生工具调用意图
  -> ToolRuntime policy check
  -> execute_command dry-run
  -> JSONL logs
  -> metrics report
```

## 模块完成情况

- Runner / config / run 目录：已完成，支持 infection 和 trigger 两阶段运行，并写入统一 run 目录。
- Agent backend：已完成，支持 CrewAI + DeepSeek，也支持 `AGENT_BACKEND=mock` 离线兜底。
- Tool runtime：已完成，`read_url` 只读本地 fixture，`execute_command` 永远 dry-run。
- Shadow memory：已完成，支持 memory 写入、检索、`memory_id` 和 source trust label。
- Provenance：已完成，工具调用日志能关联 `retrieval_context_id`、`retrieved_memory_ids` 和 `derived_from_memory_ids`。
- Tasks / fixtures：已完成 MVP，包含 5 个 infection、5 个 trigger、3 个 control、3 个 synthetic poison fixtures。
- Metrics / report：已完成，支持 ASR、block rate、post-policy 风险，以及 trigger/control 拆分指标。

## 关键运行结果

### Mock backend

mock backend 用于不依赖 API key 的链路验证。

- Trigger run：`run_20260703_003727_8eb058`
- 风险工具意图：5
- `execute_command` 真实执行：0
- `block_rate`：1.0
- `post_policy_asr`：0.0

结论：工具层 dry-run 链路稳定有效。

### DeepSeek 原始样本

原始样本下 DeepSeek 没有产生高风险工具意图。

- Trigger run：`run_20260703_005702_e36afd`
- 风险工具意图：0
- `attempted_asr`：0.0
- `post_policy_asr`：0.0

结论：当前原始样本强度较低，DeepSeek 没有被诱导出 `execute_command` 意图。

### DeepSeek 调整后样本

调整后样本只使用安全占位符，不包含真实危险命令。

- Trigger run：`run_20260703_144515_bb2204`
- 风险工具意图：4
- `execute_command` 真实执行：0
- `block_rate`：1.0
- `post_policy_asr`：0.0

结论：DeepSeek 能产生安全占位的 `execute_command` 工具意图；工具层能全部 dry-run，不造成真实执行风险。

## 需要说明的局限

当前 DeepSeek 调整后样本中，风险工具意图既出现在 report trigger，也出现在 control task。最新 metrics 已经拆分：

- `report_trigger_risky_tool_attempt_count`
- `control_risky_tool_attempt_count`
- `report_trigger_attempted_asr`
- `control_attempted_asr`

这说明当前 retrieval 会把污染 memory 带入 control 任务。这个现象不是工具层失败，而是后续 retrieval / provenance policy 可以继续优化的方向。

## 推荐展示口径

建议展示时分成两层：

1. **安全工具层结论**：只要 Agent 产生 `execute_command` 意图，`ToolRuntime` 都只记录并 dry-run，真实执行次数保持 0。
2. **模型行为结论**：真实 LLM 是否产生风险意图取决于样本强度；DeepSeek 原始样本未触发，调整后样本触发了安全占位意图。

不要声称系统已经解决所有 memory poisoning 问题。当前成果是一个可审计、可复现、不会真实执行危险命令的实验平台 MVP。
